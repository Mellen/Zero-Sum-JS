function game(boardview, scoreview, nextview)
{
    var boardview = boardview;
    var scoreview = scoreview;
    var nextview = nextview;
    var score = 0;
    var board = [];
    var rows = 23;
    var cols = 6;
    for(var col = 0; col < cols; coll++)
    {
	for(var row = 0; row < rows; row++)
	{
	}
    }
}